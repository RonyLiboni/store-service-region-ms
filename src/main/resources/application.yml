spring:
  datasource:
    url: jdbc:mysql://localhost:3306/store_service_region?createDatabaseIfNotExist=TRUE
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: false
